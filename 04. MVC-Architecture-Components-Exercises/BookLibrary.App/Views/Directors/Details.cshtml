@model DirectorDetailsViewModel
@{
    ViewData["Title"] = "Details";
}

<h2>@Model.Name Details</h2>

<ul>
    @foreach (var movie in Model.Movies)
    {
        <li><a asp-controller="Movies" asp-action="Details" asp-route-id="@movie.Id">@movie.Title</a></li>
    }
</ul>
