@page "{id}"
@model BookLibrary.App.Pages.Authors.DetailsModel
@{
    ViewData["Title"] = "Details";
}

<h2>@Model.Name</h2>

<ul>
    @foreach (var book in Model.Books)
    {
        <li><a asp-page="/Books/Details" asp-route-id="@book.BookId">@book.Title</a></li>
    }
</ul>

